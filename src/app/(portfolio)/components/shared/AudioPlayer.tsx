'use client'

export default function AudioPlayer() {
  // Return null to prevent redundant audio player rendering and 404 console errors.
  // The intro story audio is loaded and managed dynamically within the HeroSection component.
  return null;
}
